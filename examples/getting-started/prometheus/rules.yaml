groups:
  - name: checkout
    rules:
      - alert: CheckoutTrafficMissing
        expr: sum(rate(checkout_requests_total[5m])) == 0
        for: 10m
        labels:
          severity: critical
        annotations:
          summary: Checkout is receiving no traffic

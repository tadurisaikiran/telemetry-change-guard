apiVersion: tcg/v1alpha1
kind: ChangeSet
metadata:
  name: checkout-queue-metric-removal
spec:
  changes:
    - id: remove-checkout-queue-depth
      kind: metric_remove
      domain: prometheus
      from:
        domain: prometheus
        kind: metric
        name: rabbitmq_queue_messages_ready
